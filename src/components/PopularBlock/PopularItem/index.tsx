import { FC } from 'react'
import styled from 'styled-components'

const StyledPopularItem = styled.div`
  display: flex;
`

const PopularItem: FC = () => {
  return <StyledPopularItem></StyledPopularItem>
}

export default PopularItem
